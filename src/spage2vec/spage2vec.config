params {
    tools {
        spage2vec {
            container = 'gapartel/spage2vec:0.1.0'
            off = 'h5ad'
            // device for computing mapping, e.g. '0' for 'cuda: 0' change to 'cpu' for using CPU
            device = '0'
        }
    }
}

